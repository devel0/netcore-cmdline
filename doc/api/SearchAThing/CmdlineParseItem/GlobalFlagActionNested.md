# GlobalFlagActionNested property (SearchAThing.CmdlineParseItem)
if false global flag action will executed immeditaly; if true execution will deferred to nested parser

## Signature
```csharp
public bool GlobalFlagActionNested
{
    get;
    set;
}
```
